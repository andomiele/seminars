// eslint-disable-next-line import/prefer-default-export
export const rollbarConfig = {
  accessToken: '38279758a05646feb4b0aae8b1b477c1',
  enabled: process.env.NODE_ENV === 'production',
  environment: 'prod',
};
