{
  "name": "seminars",
  "version": "1.0.0",
  "description": "Multiple user chat",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start-server": "json-server ./server/database/seminars.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd frontend && npm ci"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
